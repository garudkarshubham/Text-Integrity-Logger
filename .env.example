# Database Connection String
# Connect to your MongoDB instance (Atlas or local)
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/dbname"

# Session Secret
# Key used to sign session cookies. Change this to a random string.
SESSION_SECRET="change-this-to-a-secure-random-string"

# Tamper Secret (Optional/Dev)
# Used for testing tampering status
TAMPER_SECRET_KEY="optional-tamper-secret"
